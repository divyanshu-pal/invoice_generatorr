"use strict";
// // utils/generateToken.ts
// import jwt from 'jsonwebtoken';
// const generateToken = (id: string): string => {
//   const secret = process.env.JWT_SECRET || 'secret'; // It's better to store your secret in an environment variable
//   const expiresIn = '1000h'; // Token expiration time
//   return jwt.sign({ id }, secret, { expiresIn });
// };
// export default generateToken;
